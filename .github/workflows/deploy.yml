name: Deploy to Vercel

on:
    push:
        branches: [main]
    workflow_dispatch:

jobs:
    deploy:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Setup Node.js
              uses: actions/setup-node@v4
              with:
                  node-version-file: ".nvmrc"

            - name: Setup pnpm
              uses: pnpm/action-setup@v4

            - name: Install dependencies
              run: pnpm install --frozen-lockfile

            - name: Run quality checks
              run: pnpm check

            - name: Install Vercel CLI
              run: npm install -g vercel

            - name: Debug Vercel setup
              run: |
                  vercel whoami --token ${{ secrets.VERCEL_TOKEN }}
                  vercel project ls --token ${{ secrets.VERCEL_TOKEN }}

            - name: Deploy to Vercel
              run: vercel --prod --token ${{ secrets.VERCEL_TOKEN }} --org ${{ vars.VERCEL_ORG_ID }} --project ${{ vars.VERCEL_PROJECT_ID }}
